# V₀ / inner-potential scan (extend k/kz) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans after **user approves** this plan. Checkbox steps for tracking.

**Goal:** Document how to **choose / scan** inner potential V₀ for absolute kz — still the **same k / kz skill**, not a new skill.

**Architecture:** Docs-only. **Extend** `### V₀ and convert` in `reference/k-and-kz-conversion.md`. No new `reference/v0*.md`, no new SKILL trigger, no new README skill row.

**Tech Stack:** Markdown; upstream `tools.kzconv.scan_inner_potential` / `PeriodScan` (`arpes_viewer`); PyARPES = stated V₀ + visual/periodicity check (no invent scan loop unless package API exists — verify before claiming).

**Depends on:** Item 2 (viewer kz_map prep) on `feat/kz-hv-prep`. Stay on this branch.

**Queue:** Item **3 of 8**. Framed as **extend existing**, same as Item 2.

## Global Constraints

- **One skill:** k / kz conversion.
- Never silent V₀ (already rule) — scan result still **stated source** + uncertainty.
- Upstream honesty: scan is a **weak** measurement (`PeriodScan.uncertainty`); settles by eye vs BZ / zone boundaries, not the number alone.
- `spacing` (Å along surface normal) required for viewer scan — **ask** user / lattice; do not invent `c`. Cross-link cleavage candidates later (Item 7) if relevant; this item only: ask + state.
- Package-first: `tools.kzconv.scan_inner_potential` on viewer; on `pyarpes` prefer user/literature V₀ + periodicity eye-check; **no DIY Fourier V₀ fitter**.
- Local-first; push only when asked.

---

## File map

| Action | Path | Responsibility |
|--------|------|----------------|
| Modify | `reference/k-and-kz-conversion.md` | Expand `### V₀ and convert`: sources table + viewer scan subsection + PyARPES path |
| Modify | `examples/convert_k_kz.md` | Short V₀-scan sketch under viewer / hv section |
| Modify | `reference/backend-capability-map.md` | Optional step ID `scan_inner_potential` → same doc; enrich `convert_kz` Browser cell |
| Modify | `reference/failure-modes.md` | Silent V₀=10; treat scan best as exact; invent spacing; DIY period scan on pyarpes |
| Modify | `reference/arpes-viewer-backend.md` | One line: V₀ scan in k-and-kz-conversion |
| Skip | New skill reference / README skill row | **Forbidden** |

---

### Task 1: Extend V₀ section in `k-and-kz-conversion.md`

**Files:**
- Modify: `reference/k-and-kz-conversion.md`

- [ ] **Step 1: Reframe `### V₀ and convert`**

  Keep existing “ask or state / set attrs / convert” as the **default**.

  Add **sources** table:

  | Source | When | Report |
  |--------|------|--------|
  | User / literature | Preferred | Value + citation |
  | Viewer `scan_inner_potential` | User asks scan / unknown V₀ on `kz_map` | `best` + uncertainty + spacing used |
  | Typical 5–15 eV guess | Last resort | Mark **uncertain / relative kz** — never silent 10 eV |

- [ ] **Step 2: New `#### Viewer — scan_inner_potential`**

  1. **Gate:** backend `arpes_viewer`; aligned (or EF-prepped) hv cube; user asks V₀ scan / periodicity tune.  
  2. **Need:** `spacing` (Å, repeat along normal — often `c` or `c/2`); `work_function`; optional trial grid (upstream default ~2–30 eV step 0.5). **Ask spacing** if unknown.  
  3. **Call:** `tools.kzconv.scan_inner_potential(hv, angle, energy, cube, spacing=…, work_function=…, …)`.  
  4. **Read:** `PeriodScan` — trial V₀ vs measured period; `best` crossing toward `2π/spacing`; echo **uncertainty** / weak-measurement warning from upstream.  
  5. **Settle:** prefer visual kz pattern vs BZ / zone boundaries over the scalar alone; ask user accept/edit `best`.  
  6. **Then:** pass accepted V₀ into `to_kz_cube` / attrs; store in npz `inner_potential` + assumptions text (spacing, scan vs user).

- [ ] **Step 3: PyARPES note**

  No silent invent of `scan_inner_potential` clone. If package has no equivalent → user V₀ / literature / mark relative; optional eye periodicity vs hv. If user wants automated scan on pyarpes stem → A/B/C/**D** (D = viewer if data can live there) or B.

- [ ] **Step 4: Rules / pipeline**

  One rules row: **V₀ scan** = optional step same skill; state source + uncertainty.  
  Pipeline: after EF prep → **resolve V₀** (user | scan | uncertain) → convert.

- [ ] **Step 5: Commit**

  ```bash
  git commit -m "docs: V0 scan / periodicity inside k/kz conversion"
  ```

---

### Task 2: Example + capability + failure + viewer note

**Files:**
- Modify: `examples/convert_k_kz.md`
- Modify: `reference/backend-capability-map.md`
- Modify: `reference/failure-modes.md`
- Modify: `reference/arpes-viewer-backend.md`
- Light touch: `SKILL.md` / `README.md` only if V₀ wording implies “always ask only” and omits scan — one phrase OK; **no new skill row**.

- [ ] **Step 1: Example** — sketch `scan_inner_potential` + accept `best` → `to_kz_cube`.

- [ ] **Step 2: Capability** — ID `scan_inner_potential` (step): Browser = `tools.kzconv.scan_inner_potential`; PyARPES = N/A (user/lit / ask / D); Ref = `k-and-kz-conversion.md`. Enrich `convert_kz` Browser note: after V₀ resolve.

- [ ] **Step 3: Failure modes**

  | Mistake | Fix |
  |---------||------|
  | Silent V₀ = 10 eV | Ask / scan / mark relative |
  | Treat scan `best` as exact truth | Echo uncertainty; eye vs BZ; user accept |
  | Invent lattice `spacing` | Ask user / cell |
  | DIY period-vs-V₀ loop on `pyarpes` | Package or A/B/C/**D** |
  | New “V₀ skill” in reports | Say k/kz (V₀ step) |

- [ ] **Step 4: Commit**

  ```bash
  git commit -m "docs: wire V0 scan into capability map"
  ```

---

## Verification

- [ ] No `reference/v0*.md` / new skill name.
- [ ] Grep: `scan_inner_potential` in `k-and-kz-conversion.md`.
- [ ] README/SKILL: no second skill “V₀ scan.”
- [ ] `git status` clean.

---

## Out of scope

- Cleavage / moiré / full `tools.cleavage` (Item 7 — may later supply `spacing` candidates).
- FS bend / `edge_flatness` deep dive (Item 6).
- Vendoring code; push until asked.

---

## Execution handoff

After **approve**: implement on `feat/kz-hv-prep`. Local commits. Stop; ask push.
